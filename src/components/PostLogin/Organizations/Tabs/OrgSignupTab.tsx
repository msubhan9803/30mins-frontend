import React from 'react';
import OrgSignup from '../../../PreLogin/Auth/OrgSignup';

const OrgSignupTab = ({organizationData}) => (
  <>
    <OrgSignup organizationData={organizationData} />
  </>
);

export default OrgSignupTab;
